#include "main.h"
/**
 * add - a function that adds 2 integers one and two and returns the result
 * one: first integer input
 * two: second integer input
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
